#include "main.h"

/** 
 * _abs - prints the absolute value of an integer
 * @t: the integar to be printed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}

