#include "main.h"

/**
 * _abs - computes the absolute value of an integar
 * @t: the integar for computing
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}

